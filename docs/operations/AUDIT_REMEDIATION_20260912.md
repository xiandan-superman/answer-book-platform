# 2026-09-12 审查整改跟踪

基线：078f6e5 加本地未提交修改。用户授权按风险顺序整改；迟到的重生成候选保存回原任务，使用原编辑版本检查，不抢占新页面。以下“已改”不等于完整验收或发布。

| 编号 | 状态 | 改动/仍需验证 |
| --- | --- | --- |
| 9 | 已改，验收中 | 单题/批量固定保存目标及编辑版本；整批冻结模型和输入，切页仍继续原任务。未保存候选按原任务分开保留本地草稿，不覆盖人工草稿。原函数 A→B 时序通过；多标签与存储配额失败尚未实测。 |
| 10 | 已改，验收中 | 固定蓝图对象、会话和原计划项内容；丢弃失效响应。仍需真实交互复验。 |
| 17 | 已改，验收中 | 选项/公式使用结构化 JSON 编辑，保留换行及所有元数据；populate→apply 原函数无修改保存验证通过。旧草稿非 JSON 不强制猜测转换，需手工整理；真实复杂 Word 页图未验收。 |
| 18 | 已改，验收中 | 解析器阻断绝对包外、相对越界和符号链接；资产审计记失败；候选图片读取前复查历史 content_list 来源路径。需全面调用链和历史索引来源变体复核。 |
| 13 | 已改，局部验证通过 | 结构化终态错误不再被当成网络查询异常重试；原函数三种终态各一次退出已验证。 |
| 16 | 已改，局部验证通过 | 首次 await 前序列化完整创建请求，索引确认、材料确认和模型预检共享原快照。原函数确认阶段 A→B、预检阶段 B→C 后仍只提交 A 验证通过。 |
| 11 | 已改，验收中 | 历史点击请求递增会话；旧保存响应不更新当前视图。 |
| 12 | 已改，局部验证通过 | runTask/taskStatus/taskQuality 成功和失败分支检查导航及任务输入身份，六种原函数迟到场景通过。 |
| 14 | 已改，验收中 | 生成失败 catch 和 finally 校验会话，不把新页面切回旧蓝图或改写新按钮。 |
| 15 | 已改，验收中 | 创建响应不能抢占新观察指针；旧轮询不更新编号/进度/回调。 |
| 20 | 已改，待 Windows 实机 | atomic_write_json 父目录创建先经 long_path；不能以 macOS 测试替代 Windows 实机。 |
| 19 | 已改，局部验证通过 | ids=[] 无删除；只有 None 表示全部，删除替身验证通过。 |
| 21 | 已改，待 Windows 实机 | 三个 MinerU 替身保留本机 Path 类型；长路径期望先规范化，原子写入替身委托真实适配器。没有新增 Windows 泛化跳过。 |
| 1 | 已改，局部验证通过 | 环境摘要与模型卡片同用路线健康源；连接测试携带角色协议、按协议去重并重新读取状态。浏览器正常状态口径一致；非阻断选模保持。 |
| 2 | 已改，局部验证通过 | 导出恢复入口默认折叠，展开列表限制高度；仍保留全部恢复操作。真实浏览器合成记录关闭高度 69.5px、展开 93.5px；控制台无错误。 |
| 3 | 已改，验收中 | 全部题目导出 scope=all；缺失败题时仍标 selected，不冒充完整交付。 |
| 4 | 已改，验收中 | 预估名采用本地时间，完成指针使用服务端文件名并保留原创建/过期时间；恢复列表优先显示服务端文件名。 |
| 5 | 源码已核验 | provider_control 从 provider_capacity_snapshot 共享池统计，非逐路线累加；未以本轮测试证明旧服务已加载新后端。 |
| 6 | 已改，验收中 | 同名缓存显示短标识，悬浮和无障碍标签包含完整标识、来源和缓存更新时间；索引也提供时间，不把它称为原文件创建时间。 |
| 7 | 待复现/非默认缺陷 | 用户已明确只在任务详情弹结构确认，其他页面只提示人工确认；材料确认另有 createTask 弹窗，不可混同。 |
| 8 | 等待材料 | 已向用户索取原始多题 DOCX 及期望配置；未有证据前不改全局排版合同。 |

## 边界与验证

- 无真实模型调用、未删除用户数据、未重建用户任务、未发布。
- 新增 `test_audit_async_20260912.py` 原函数时序测试及 `test_audit_boundaries_20260912.py` 合成文件边界测试。
- 新增定向原函数时序、前端合同 138 passed；缓存/路径/取消/原函数组合 58 passed。未进行真实模型调用。
- 第一轮完整门禁发现任务取消字典的类型错误，补充类型声明，不改变取消行为；第二轮 2370 passed / 1 failed（批量调用静态断言仍为旧签名），已更新断言并补行为测试。最终门禁结果下附。
- 后端修改需受控重启才能生效；不在有用户任务运行时贸然重启。

## 跨场景影响矩阵

| 场景 | 适用改动与证据 | 未覆盖边界 |
| --- | --- | --- |
| 真题解析/教材引用定位 | 创建模型/材料快照，状态/质量/启动迟到隔离；原函数模拟时序和共享测试 | 真正模型输出和新任务端到端未调用 |
| 按题出题 | 单题/整批重生成原任务保存、计划项时序、编辑无损和终态退出；原函数与后端回归 | 多标签点击、存储配额耗尽未实测 |
| 知识点出题 | 与按题共用上述处理函数；冻结原 source_mode/request，而非切页后的配置 | 大型真实题组和生图未调用 |
| 历史恢复 | 保存/读取/撤销响应校验会话，候选保留原版本，恢复通知折叠 | 冲突草稿为浏览器本地存储，配额不足时不能保证保存 |
| Word/PDF | 选项换行、公式 ID/display 无损；Word scope、文件名、恢复指针修正 | 第 8 项原始多题 DOCX 缺失；未做 Word GUI/打印/PDF 页图验收 |
| 教材共享输入 | 新包入口拒绝越界；历史 content_list 图块在请求组装前复查；边界合成测试 | 未遍历全部外部 OCR 目录变体；含父级引用的包会明确拒绝，不静默丢图 |
| 存储/检查点 | [] 空选择不删除，None 全部合同保持；父目录经过 long_path；本机测试 | Windows 无全局长路径配置的实机仍需验证 |
| 供应商/环境 | 状态统一来源，协议身份保留；共享并发按池计数；浏览器正常状态冒烟 | 不重新测试真实供应商，不改变已登记模型能力或故障阈值 |

## 交付状态

- 源码已修改；本机回归与有限浏览器冒烟，不等同全部用户验收。
- 未 Git 提交、推送、正式源码发布或桌面包发布。
- 本轮未重启正式服务、未删除用户缓存或任务。

## 本轮门禁结果

- `/private/tmp/answer-book-release-py311.uvjOJc/bin/python scripts/run_quality_gates.py --full`：通过。2371 passed，17 E2E deselected，12 Pillow 弃用警告；覆盖率 72%；Ruff、Mypy、版本、公式、许可、项目完整度均通过。
- 完整门禁后仅补充终态清除观察指针也校验会话（与进度/编号一致），并单独运行相关定向回归；不把完整门禁说成浏览器端到端或 Windows 实机通过。
- `git diff --check` 和 `node --check web/app.js` 通过。
- 验收仍未全部关闭：第 8 项缺原文；20/21 缺 Windows 实机；9/11/14/15 的多标签/存储异常、17 的复杂真实交付及18的更多旧包目录变体仍需补充。
