# MinerU runs in an isolated managed Python 3.11 environment.  Keeping it out
# of the web process prevents its large OCR/ML dependency graph from changing
# the deterministic document-generation runtime.
mineru[pipeline]==3.4.5
