# 版本更新记录

本文件记录普通用户可见的正式版本变化。每次发布必须先增加与 `APP_VERSION` 一致的条目；GitHub Release 和应用内更新说明均从对应条目生成。

## [0.9.59] - 2026-09-19

### 任务管理

- 修复展开运行详情后任务列表停止同步的问题；任务完成、失败或取消后会及时刷新，并保留已展开区域。
- 失败或取消的真题任务按已持久化的当前阶段进度显示，任务管理与详情页使用同一进度事实。

### 网络连接可靠性

- 对智能路由、模型供应商、更新包和必要运行资源使用直连优先；仅在 DNS、TLS、超时或连接中断时回退到系统代理。
- Cloudflare 智能路由预约增加有界重试，实际模型请求在原有硬超时和重试上限内先直连、失败再使用系统代理；鉴权和参数错误不重复发送。

### 验证范围

- Python 3.11 完整门禁通过：2576 项测试，覆盖率 70%；隔离 Chromium 发布回归连续两次通过。
- 未发起收费模型请求；Windows 实机、复杂真实 Word/PDF 场景和桌面安装包未在本次重跑。

## [0.9.58] - 2026-09-19

### 智能路由与任务管理

- 修复点击“智能路由”分组后仅切换显示、实际仍提交上一个火山方舟模型的问题；切换模型分组时会同步选中该分组的候选模型。
- 修复从任务管理打开任务时短暂显示上一个任务进度、结果或文件的问题；页面会先清空旧任务内容并只加载当前选择的任务。
- 增强智能路由服务对 Cloudflare 配置类型和上游兼容错误的处理，避免有效配置被误判或单条不兼容路线阻断切换。

### 手动更新

- 修复源码版更新完成并重启服务后，旧网页因本机令牌变化持续停在 99% 的问题。
- 补全解压、备份、依赖安装和启动阶段的进度收口；新版启动后自动完成至 100% 并刷新页面。

### 验证范围

- Python 3.11 完整门禁通过：2569 项测试，覆盖率 70%；Cloudflare Worker TypeScript 检查通过。
- 未发起收费模型请求；Windows 实机、复杂真实 Word/PDF 场景和桌面安装包未在本次重跑。

## [0.9.57] - 2026-09-19

### 智能路由配置

- API 配置页只保留统一的 Cloudflare 智能路由入口，不再将 Claude 智能路由显示为独立 Key 配置。
- GPT、Gemini、Claude 和生图智能路由继续共享 Cloudflare 地址与用户访问 Key，并在任务模型选择中分别显示。

### 验证范围

- 前端语法和配置页面相关契约测试通过；本次不修改 Cloudflare 云端 Key、路线或用户数据。

## [0.9.56] - 2026-09-18

### 路由与公式显示

- 将 Gemini、GPT、Claude 和生图智能路由统一归入独立的“智能路由”分组。
- 修复选择模型后模型列表跳回顶部的问题，保留当前分组和滚动位置。
- 优化 LaTeX 公式渲染；单个公式失败不会影响整页其他公式，并保留可读的源码提示。

### 验证范围

- 前端相关测试通过，线上 Claude 智能路由真实最小请求验证通过。

## [0.9.55] - 2026-09-18

### 模型调用可靠性

- 修复 Responses 工具回合在没有工具调用时仍重复循环的问题。
- 对残缺 JSON 只进行一次基于干净上下文的格式修复，避免异常输出反复回灌导致上下文膨胀。
- 增加输出边界诊断事件，并保留原始请求与响应的脱敏诊断记录。
- 保留既有答案解析、公式、图片资产和结构质量门禁，修复候选不会覆盖已通过的内容。

### 模型路由与任务展示

- 完善多模态模型路由、生图路由和实际模型调用记录。
- 新增灵算与 WawAPI 的 Claude 分组，接入 `claude-opus-5`、`claude-sonnet-5` 及 Claude 智能路由；不接入 fable 和 Top-API Claude。
- 更新任务模型名称、路由状态和相关前端展示。

### 验证范围

- 本地工具循环、审查修复、答案生成和流水线定向回归通过。
- 完整 Python 3.12 本地门禁通过：2552 项测试；Cloudflare Worker TypeScript 检查通过。
- Claude 智能路由完成线上最小真实请求验证，返回正常；本次未提交用户机任务数据。

## [0.9.54] - 2026-09-16

### 模型选择与目录

- 教材引用、真题解析同级选择，保留独立请求方式与思考强度，生图必选；移除重复环境检查和独立读图、复核设置，复核跟随解析模型。
- 按模型家族快速选择，压缩卡片与页面留白，改进任务页面布局。
- 接入灵算 DeepSeek V4.1 Flash，按官方说明登记多模态；移除旧纯文本 DeepSeek 和指定五个百炼型号。

### 解析、修复与任务展示

- 区分单选、多选题并贯穿题型确认与审查。
- 修复答案审查后已采用图片的位置和图注丢失；Windows 文件保存遇临时占用时有界重试。
- 失败任务展示有证据支持的具体原因，区分缺图、保存失败与模型连接问题。
- 修复 Word 格式页面脚本安全策略与接口错误响应，并完善相关回归。

### 验证范围

- 完整 Python 3.11 门禁通过：2438 项测试，覆盖率 70%；模型短测不代表完整教学任务质量验收。
- Windows 实机与复杂真实 Word/PDF 场景未重跑；本次为源码更新版，不另发桌面安装包。

## [0.9.53] - 2026-09-12

### 灵算接入与模型配置

- 灵算全部分组统一使用 Edge 接口；新增国模分组十个模型，共用该组独立 Key。
- 模型与供应商支持手动测试并更新记录，不同模型独立发起；完成行在可见后提示 7 秒。
- 请求方式按具体路线展示；新建真题默认教材与主解析模型为灵算 GPT 5.6 Sol，生题先选择模型再提交原题。

### 任务流程与可靠性

- 教材依据优先占用同供应商并发，空余容量生成答案草稿；独立供应商分别调度，含图试卷按逐题输入条件准入，最终仍须证据绑定和质量审查。
- DOCX 自动上传，原页显示进度、删除与下一步；识别分值预填，未知留空，增加 5/10/20 分快捷选择。
- 统一可反馈反查的 RW 任务编号；修复跨任务异步结果污染、版本冲突手工合并、Word 恢复记录重名及答案汇总重复题干。
- 缓存支持按项清理；程序检查聚焦结构、覆盖、引用、资产、公式等客观合同，保留模型内容审查。

### 验证范围

- 完整 Python 3.11 门禁及 Chromium 关键流程通过；国模十条路线完成最小 JSON 连通测试，不代表长任务、图片或并发容量验收。
- 缺少原始材料的历史审查项及部分 Windows 实机、复杂 Word/PDF 场景仍待验证；本次为源码更新版，不另发桌面安装包。

## [0.9.52] - 2026-09-11

### 本地运行与多模态答案流程

- 平台任务统一为本机运行，移除云端服务器、混合执行和共享教材库相关入口、配置与同步功能；已有本地任务、教材、配置和输出不会被删除。
- 支持多模态的主模型逐题接收结构化文字和完整题面快照；题目另有高清原图时会额外附带原图，不支持多模态的模型仍只接收文字。
- 是否必须生成答案图片由主模型决定。程序规则只能要求主模型重新考虑一次，不能独立判定结果失败；主模型明确调用生图但有限重试仍无有效图片时，任务会停止并说明生图供应商、模型及失败责任。

### 质量、可靠性与交付体验

- 选择题和判断题题干统一补充中文答题括号 `（      ）`，首次生成、重生成、网页展示与 Word 导出保持一致。
- 修复任务记录并发写入、异常 JSON 瞬时读取、文件访问边界、大文件传输和本地/LAN 权限隔离；普通用户页面不再展示难以理解的原始监控日志。
- 前端按阅读、任务、配置和监控场景重新组织宽屏空间，完善弹窗键盘焦点、空状态、加载骨架、字号和状态语义，监控大画布仍保留高信息密度。
- 发布前以一题真题解析和一题生题完成真实轻量验收；另一个两题真题任务在模型内容缺步时被质量门正确降级，没有掩盖成完整通过。

## [0.9.51] - 2026-09-11

### 供应商状态、止损与责任说明

- 新增供应商运行中心：按火山方舟、DeepSeek 官方、阿里云百炼、灵算和 WawAPI 聚合展示已接入模型、能力状态、有效并发和近期观测；具体模型、协议、账号指纹及错误责任按需展开。
- 每条路线按“供应商、账号、模型、请求协议、能力”独立记录。连续供应商异常会降并发并及时停止无效重试，一次精确成功即可立即恢复，不再用一个模型的波动误伤同供应商其他模型。
- 任务开始前验证本次实际需要的文本、视觉、工具和生图路线；真实任务发现状态不一致时只复核出错能力。供应商、用户账号配置和平台请求问题分别说明，零产出的路线故障不再显示为普通完成。
- 后台文本检测按到期时间摊平，生图只允许用户手动检测；公开模型列表只生成待审核候选，不会自动上线新模型、删除既有模型或切换协议。

### 模型、协议与并发能力

- 任务可为已验证支持的模型选择 Chat Completions 或 Responses，请求协议与思考强度一同固化到任务、恢复和重试流程，不静默换协议。
- 接入 DeepSeek 官方 `deepseek-flash`，替换过期官方直连模型；灵算增加已验证的 Gemini 3.8 Flash 通道；WawAPI 保留模型完成重新验证。
- 文本与图片供应商容量分别记录，生产并发使用实测档案并继续接受任务反馈动态降级；短期测试结果只代表测试时点，不作为长期服务承诺。

### 任务预算、教材证据与恢复

- 单题模型预算从实际取得供应商名额后开始，排队与执行分开计时；任务总预算按题量、预计调用和供应商容量计算，并为答案生成保留最低预算。
- 修复并发排队、恢复单探针和连续流式读取超时的状态竞争；持续服务商异常达到合理重试上限后停止任务并给出可操作原因。
- 教材证据候选按知识点轮询分配，确保各知识点获得候选；扩展空结果不会删除已确认依据。
- 已完成题目、检查点和可交付成果继续保留；真题解析、按题出题、知识点出题以及历史恢复共享相同供应商运行策略，不降低内容和 Word 交付门禁。

## [0.9.50] - 2026-09-09

### 任务身份、状态与部分成果

- 出题任务使用跨分析、蓝图和生成阶段稳定不变的任务 ID；每次后台执行另有执行 ID，诊断和失败提示不再混用两者。
- 有实际且通过完整性检查的候选 Word 时才会显示“带问题完成”；没有候选文件的短路或历史异常保持失败/需处理。
- 正式整套 Word 继续执行严格质量门；已通过的单题可按稳定题目 ID 导出，真题解析的有效分题成果继续提供版本化 ZIP。

### 上传、蓝图与生图体验

- 按题出题、知识点出题和教材库统一禁止 TXT/Markdown 文件上传；图片、PDF、Word 仍可上传，纯文字可直接粘贴。
- 知识点蓝图的语义范围提醒完整展示并要求用户逐项确认；确定性结构、身份、题型和必填字段错误仍会直接阻断。
- 正式退役 legacy 程序生图链路；主模型图片工具只在已确认蓝图明确要求生成题干新图时启用，材料分析、蓝图规划和纯文字出题不再要求生图配置。
- 真题四步导航禁止提前跳步并补齐无障碍状态；后台等待页可分别复制任务 ID 和执行 ID。

### Windows 与公共契约修复

- Windows MinerU 首次安装、长路径检查点、SQLite 连接、进程锁诊断、字体 MIME、UTF-8 质量门和 LAN 兼容入口得到统一修复。
- 创建任务会在模型调用前检查 Python 3.11、输入路径和模型协议；公共错误分类、分值短语、Unicode 公式、Word 成功响应及最终验收状态保持一致。

## [0.9.49] - 2026-09-09

### 教材引用定位与复用

- 辅助工具新增“教材引用定位”，只交付每题知识点和已核验教材页码，不复制教材原文；支持暂停、恢复、取消和下载。
- 同一教材的索引与页码身份可被多套试题复用；用户可直接选择已处理教材后只上传试题。
- 教材检索增加章节与知识点语义范围一致性检查，减少二元/三元、二维/三维等跨范围误引，并优先保留不同已核验页面的证据。

### 真题解析并行与模型配置

- 纯文本真题可同步处理教材依据与无引用答案草稿；依据完成后仍会重新绑定、审查和修正，不降低原有质量门。
- 真题解析可分别为“教材依据”和“题目解析”选择模型与思考深度；按题出题和知识点出题各保留一个主模型及一次深度选择。
- 任务会固化实际模型、请求类型和思考深度；GPT 文本路由统一使用 Responses，其他模型按已实测的精确服务商通道登记。

### 全平台并发治理

- 真题解析、两类生题和辅助工具共用服务商通道的公平队列、容量上限和动态冷却，避免同一 Key 被多个任务各自开满。
- 运行监控可查看每条模型通道的活动模型、运行/上限、等待和冷却数量；WawAPI 的 GPT、Gemini 和 Grok 通道使用已实测的默认并发值。

## [0.9.48] - 2026-09-06

### 题目生成与恢复保真

- 完整移除练习生题后的独立模型语义复核：它不再发起调用，也不再影响历史任务状态、页面提示、导出缓存或 Word 候选说明；结构、来源、边界、公式和交付检查继续保留。
- 严格原文填空现在保存可核验的来源片段与空位映射，只围绕实际选定片段核对范围；导出继续保留已确认的来源编号和范围，不会为了凑覆盖自动补绑来源。
- 练习生成只在用户明确要求时强制单题跨来源；已保存的综合练习不再自动补入连接角色或第二来源。
- 修复模型工具循环中可明确识别的 LaTeX 控制转义，并保留未知控制字符的拒绝与诊断；后续已定界公式不会被前一条未闭合百分比公式跨越改写。
- 恢复练习题时按确认蓝图中的计划 ID 校正题号与题目身份，避免局部生成结果带入重复题号。

### Word 交付与运行可靠性

- 需要人工复核的练习 Word 将完整提示作为可分页正文呈现，避免提示因标题或页眉布局被裁切；候选仍明确标记为待复核。
- 题目和答案交付流程增强了检查点、增量恢复、公式保留与结构化内容核验，减少中断后重复处理和保存后字段遗漏。
- Windows 持久化配置与发布前依赖校验增加回归覆盖，源码发布流程继续排除用户数据、凭据、任务记录和运行输出。

## [0.9.47] - 2026-09-05

### 任务稳定性与结构化输出

- 结构化模型输出改为每次请求独立携带 JSON Schema，并由本地 Pydantic 最终校验；修复多个并发任务首次调用时可能出现的结构化注册冲突。
- 模型返回结构暂不合格但仍在自动纠正时，不再提前显示“任务失败”；只有有界纠正最终耗尽才报告失败。
- 任务进度接口偶发读取失败时继续自动刷新，不再让仍在后台运行的任务被误认为中断；较长时间没有心跳时改为明确提示继续等待。
- 任务中心先展示任务基础列表，再异步补充运行详情，减少新任务进入页面后延迟数秒才出现的问题。

### 真题结构、作图流程与模型

- 清理 MinerU 泄漏的下划线展示标签，同时保留留空线和数学比较符；大题总分不再错误填入每一道小题，题型和分值仍必须由用户确认后继续。
- 真题解析及两种练习入口统一使用主模型自主生图流程，不再展示或新建历史代码绘图模式。
- 灵算 OpenAI 增加已实测的 GPT-6 Astra 选项；任务列表继续以实际调用账本显示真实模型。

### 历史任务清理与启动体验

- 平台启动后任务超过 40 条时检查较旧任务，并可按“已有下载、失败、进度不足 60% 或用户停止”等条件推荐清理；也可清理排序 40 条以后的全部安全候选。运行中、排队、暂停和等待确认任务不会进入清理范围。
- 清理操作会真正删除任务记录和对应受管文件，完成后立即刷新任务列表；弹窗可以直接关闭，不会自动删除。
- 首次启动或版本更新需要准备依赖时，启动器现在显示待处理组件数量、当前组件、安装阶段和最近活动；失败会给出脱敏原因、重试入口与日志位置。

## [0.9.46] - 2026-09-05

### Word 交付与图片流程修复

- 真题解析与辅助功能中的题目解析恢复题目结构、题型和分值人工确认弹窗；用户确认后任务才继续生成。
- 删除混合云任务转交机制、配置和界面开关，所有解析任务统一在本机运行。
- 任务管理中的真题/题目解析名称优先显示答案生成阶段账本记录的真实调用模型；首次调用前显示用户配置的答案模型，不再固定显示基础 DeepSeek 路由。
- 真题解析只交付 Word，不再生成 PDF 和页面 PNG，也不再因缺少 PDF 阻断交付；旧任务的渲染参数仍可兼容，历史文件保留。
- 移除历史独立生成图视觉审查及后置修图，保留负责内容的主模型看图采用流程，以及图片缺失、损坏和引用检查。
- Word 生成统一使用 Pandoc C 引擎，淘汰 A/B 及其回退路径；新任务和历史重建都使用 C，保留反应箭头条件及原有排版合同。环境检查准备固定版本的官方运行时并验证压缩包与二进制哈希。
- 取消默认固定的整任务 token 硬上限，继续统计实际用量；保留调用次数、时长、传输重试、修复轮数和工具循环等防失控限制。显式配置的正数 token 预算仍有效。
- 修复公式反复规范化产生嵌套、正文分组括号被截断，以及已生成图片在 Windows 保存时的文件同步问题。
- 改善灵算多协议通道的共享并发控制及有界传输重试；图片工具采用状态、诊断和修复候选校验保持一致。
- 已有内容警告继续随 Word 保留，不会因文档转换成功而自动升级为正式无问题验收。

## [0.9.45] - 2026-09-05

### MinerU Windows OCR 依赖修复

- 显式补齐 MinerU 3.4.5 pipeline 内置 OCR 代码使用、但上游未声明的 `six` 依赖；干净 Windows 环境的 pipeline 引擎导入自检可正常完成。
- 修正 Word 浮动图片在 MinerU 结果中出现在纯题号之前时被误挂到上一题的问题；图片题现在保留图片输入，旧检查点会因题目分组策略升级而自动失效并重新解析。
- 新增题目输入完整性门禁；没有题干、图片、表格或子题的条目会在调用模型前明确停止，不再延迟到知识检索阶段才以“无候选”报错。
- 清除 MinerU 在完整大题标题外添加的 Markdown 加粗标记，恢复大题分区和题号身份，不再把多个大题误并到第一大题。

## [0.9.44] - 2026-09-04

### MinerU 真实解析修复

- MinerU 隔离环境改为安装官方 `pipeline` 可选依赖，补齐 `torch` 等本地推理组件；解析命令显式固定为 pipeline 后端，不再受上游默认 hybrid 后端变化影响。
- 运行时标识现在同时绑定 MinerU 版本、Python 3.11、依赖文件摘要和 pipeline 配置；任务开始前会实际导入 `torch` 与 pipeline 模块自检，不再只凭 CLI 文件存在就误判安装成功。

## [0.9.43] - 2026-09-04

### Windows 首次启动修复

- Python 3.11 虚拟环境如果因 Windows `pythonw.exe` 启动而缺少 pip，启动器现在会自动调用 Python 自带的 `ensurepip` 修复并复核，再安装锁定依赖；不需要删除其他 Python 版本或用户数据。

## [0.9.42] - 2026-09-04

### Python 3.11 与 MinerU 安装修复

- 启动器现在固定选择 Python 3.11，不再把 Python 3.12、3.13 或 3.14 误认为平台运行环境；其他 Python 版本可以继续并排保留，无需卸载。
- 检测到过去由错误 Python 版本创建的 `python-env-py311` 时，会先改名保存为带时间戳的隔离备份，再创建真正的 Python 3.11 环境，不会覆盖任务、教材、Key 或历史输出。
- MinerU 使用独立的 Python 3.11 专用运行目录；不兼容解释器会在环境检查和任务解析前被明确拦截，不再进入 pip 后输出大段安装错误，也不会产生模型调用或 Token 消耗。

## [0.9.41] - 2026-09-04

### 文档执行与教学排版

- Word 执行新增 OfficeCLI B 引擎并设为默认，按“创建、原子批处理、保存、校验、关闭”完成真题与练习文档；现有 Python/OMML A 引擎完整保留，可通过配置明确切回，B 失败不会静默换 A 掩盖问题。
- 两个引擎继续执行平台自己的试题、答案、公式、图片、表格、页眉页脚、页码和教学排版规则；公式、图片和最终内容门禁未通过时不交付。

### 解析、结构化纠错与数学校验

- PDF/DOCX 主解析统一接入 MinerU 3.4.5，并在用户数据目录按需准备隔离运行环境；解析失败会明确停止，不再静默回退到能力较弱的旧解析链。
- 真题答案和两类练习生成增加 Pydantic 正式结构合同；Instructor 负责把结构校验错误回灌给同一模型并最多纠错一次，调用仍计入原任务预算、并发和用量记录。
- 数学一致性增加 SymPy、latex2sympy2_extended 与 Math-Verify 隔离判等，只补充原有数值规则无法判断且符号边界安全的场景，不替代单位、百分数和教学业务规则。

### 模型用量、稳定性与运行环境

- 模型调用上限改为按任务类型、实际题量和教材证据链估算，默认冻结在预估使用量约 180%，同时保留 120 次下限和 500 次硬上限；失败调用、Token、耗时和并发限制仍独立计数。
- 灵算并发进一步收紧；LiteLLM 仅以默认 10% 做有界影子对照，不重试、不替换正式响应，队列满或影子失败均不影响用户任务。
- 源码运行和正式发布统一升级到 Python 3.11；首次使用 MinerU、OfficeCLI 或新增校验能力时可能需要下载并准备对应隔离依赖，旧运行环境保留用于回滚，不包含或覆盖 API Key、任务、教材、日志和历史输出。

## [0.9.40] - 2026-09-03

### 模型失败恢复与大文件输入

- 超时、连接中断、限流和服务端短时故障使用原服务商、原模型、原协议和原思考深度有界重试；不通过换模型、降思考或删减教材证据换取成功。
- PDF、Word、图片和文本材料保留原文件、结构化文本和页面视觉表示；局部解析失败会判断同源表示是否足够，不再静默忽略 Word 公式、表格、内嵌图或扫描页。
- 工具调用和多轮复核增加耐久恢复边界；结果不确定的外部操作不会盲目重放，修复始终以最新有效候选为输入。

### Word 公式与交付完整性

- 真题解析和两种生题入口共用公式分隔解析；跨行 `$$...$$` 会作为一个完整公式转为 Word 原生对象，不再被分段拆碎。
- 生题的题干、选项、答案、解析、知识点、表格和图示文本统一执行 Word 公式预检；无法转换时给出题目和字段位置，不会静默输出原始 LaTeX。
- Word/PDF/ZIP 修复、验收和公式交付改为先生成候选、重跑内容门禁并核对哈希，通过后再原子替换正式文件，失败不覆盖既有成品。

### 发布与数据安全

- 平台级修复现在必须同时审查真题解析、按题出题、知识点出题、历史任务恢复和最终交付，避免只修复一个页面或一种任务。
- 更新仅替换程序源码，不包含或覆盖 API Key、任务、教材、日志和历史输出。正式源码版继续由 `main` 完整门禁通过后自动创建标签与公开附件。

## [0.9.39] - 2026-09-01

### 生题设置与结果编排

- “补充要求”改为可直接编辑、也可展开多选的组合输入框；已保存要求可在下拉内直接新建、编辑和删除，长文本保持单行省略。常用项保存在系统用户数据目录，源码更新不会覆盖。
- 单选题和多选题在最终编排时由程序随机调整选项顺序并重建 A、B、C、D 标签，减少正确答案长期集中在 A 的偏差；已保存题目、页面刷新和导出不会重复打乱。
- 多图材料不再把阶段建议的 8 张当作硬上限；在已有 24 张材料接收边界内，短材料保留完整跨图理解，长材料按文本段落分组并保持稳定的图文编号。

### 模型质量与错误恢复

- 通用 JSON 重试现在固定使用原服务商、原模型、原协议、原请求内容和原思考深度；不再通过关闭思考、静默换模型或截断教材/真题证据来换取成功。
- 模糊 400 不再进入通用重试或协议切换；灵算已有的失败题单次同路由补偿和批次拆分保留，已成功题不受影响。
- Responses/Messages 回退默认关闭；只有用户或通道已显式允许、请求为纯文本、模型不变且上游明确返回端点不支持时，才允许一次 Chat 兼容请求。图片、工具、超时、429、5xx 和 JSON 内容错误不会触发跨协议回退。

### 页面可用性与发布边界

- 长题编辑器保持居中，标题和底部操作不会被滚动移出视口；结果页进入后回到顶部流程上下文，题目选择入口和需复核风险信息更清晰，技术详情继续可展开查看。
- 锁定 Python 3.11 完整本地测试通过 1943 项、按配置排除 16 项，整库分支覆盖率 70%；Python 编译、版本一致性、公式转换、Ruff、Mypy、第三方声明和项目完整性检查全部通过。本版本未发起新的付费模型或生图请求。
- 更新不会包含或覆盖 API Key、任务、教材、日志和历史输出；正式源码版继续由 `main` 门禁通过后自动创建标签与公开附件。

## [0.9.38] - 2026-08-30

### 灵算 Gemini 3.7 与自主配图

- 灵算 Gemini 3.7 Flash 只显示一个模型选项，底层固定使用供应商真实可用的 `gemini-3.7-flash-medium`，不再发送会返回 404 的无后缀模型名。
- 旧版保存的无后缀、low 和 high 值自动迁移到 medium；灵算 Google 供应商的整体默认仍保持 Gemini 3.6 Flash，升级不会强制改选。
- Gemini 3.7 medium 已完成原生工具调用与盲图回看验证：主模型可自主调用已配置的图片模型，检查真实像素后再决定是否采用。图片像素仍由独立图片服务生成，需要有效的图片 API 配置。

### 任务、结果与运行监控

- 任务管理增加名称、材料、模型和文件搜索，补齐“已暂停”筛选，并精简等待确认、失败和终止态任务卡中的重复状态与技术信息。
- “运行详情”可独立展开模型请求次数和等待上限，不再因点击折叠标题而误打开整个任务。
- 零题、部分完成和需复核结果现在使用准确的交付语义；失败占位不再显示无效编辑菜单，长结果侧栏使用连续序号并保留原题号，需复核的正式交付权限保持不变。
- 蓝图来源与审查技术信息默认收起，内部风险改为可读提示；刷新、磁盘统计和清理等操作改为可辨识按钮，异步执行期间防止重复点击并显示进行中反馈。

### 发布边界

- 锁定 Python 3.11 完整本地测试通过 1921 项、按配置排除 16 项；隔离数据目录的 Chromium 端到端测试通过 16 项。正式发布门禁另行覆盖 Python 编译、版本一致性、公式转换、Ruff、Mypy、第三方声明、项目完整性、分支覆盖率与源码包反向验证。
- 本版本不包含或覆盖 API Key、任务、教材、日志和历史输出。发布前发起了少量灵算 Gemini 文本与受控工具闭环验证，未发起付费图片生成请求。

## [0.9.37] - 2026-08-30

### 模型失败恢复与诊断

- 不带明确错误字段的通用 HTTP 400 不再阻断整份任务：批量失败会直接拆成单题，单题失败只影响当前题，已成功题和后续题继续保留。
- 失败状态区分为当前题失败、服务暂时异常、当前路由阻断和需要修改配置四类；只有 Key、模型/Endpoint 或明确不支持参数等确定性配置错误才会全局快速阻断。
- 灵算的单题模糊 400 仅在原供应商、模型、协议、消息和参数不变时补偿重试一次；不默认切换模型或供应商。私有诊断增加服务商错误字段和 request ID，用户可见错误仍保持脱敏。

### 模型配置与生成图片

- 灵算 Gemini 3.7 Flash 与 3.6/3.5 统一为单个模型选项，不再把 low/medium/high 显示为三个模型；旧本地配置会自动迁移到无后缀的 `gemini-3.7-flash`。
- `thinking_minimum=minimal` 表示最低思考深度为 minimal，实际思考由模型在该路由支持范围内处理；不向用户重复暴露三个别名。
- 生成或修改中文题目图片时，标题、说明和自然语言标注将优先使用简体中文，同时保留数学符号、字母标记、公式、单位和专业缩写；修复和重新生成也保留同一要求，不增加额外模型调用。

### 任务、审查与交付体验

- 任务管理使用“等待我确认、结果需复核、未完成”等结果语义，结果页先给出唯一交付结论；失败、暂停、取消和等待确认页不再使用“仍在运行”或活动进度语义。
- 正式交付包继续使用既有 `delivery_ready` 合同：可交付但需复核的任务仍可下载；主要 Word/PDF 与复核/过程文件分组展示，不在普通视图暴露内部门禁代码、原始模型 ID 和完整任务 ID。
- 按题蓝图长文本可换行编辑，从任务管理打开审查时保留标题与步骤上下文；API 配置、运行监控、Word 审查、教材列表和长题结果页的桌面端布局进一步减少遮挡、空洞与重复提示。

### 发布边界

- 锁定 Python 3.11 完整本地门禁通过 1914 项、按配置排除 16 项，整库分支覆盖率 70%；Python 编译、版本一致性、公式转换、Ruff、Mypy、许可证和项目完整性检查全部通过。GitHub 还会独立执行 Python 3.9/3.11、Chromium 和 macOS/Windows 源码锁定门禁。
- 本版本不包含或覆盖 API Key、任务、教材、日志和历史输出；未发起付费模型或生图请求，灵算 Gemini 3.7 Flash 无后缀路由的原生工具调用仍明确标记为待验证。

## [0.9.36] - 2026-08-29

### 执行记录与质量诊断

- 模型请求增加耐久调用身份和结果配对记录，可区分尚未请求、已请求后失败、结果被取消丢弃及记录异常；提示词合同、重试原因、模型/协议变化和实际等待时间以脱敏摘要关联，不保存提示词、题目、教材或模型响应正文。
- 新增只读任务状态投影与不变量报告，分别观察生命周期、内容完成度、页面进度、调用/重试配对、提示词覆盖和资产完整性；这些报告不修改任务状态、不放宽质量门，也不把历史证据缺口误报为真实业务失败。
- 为主要模型阶段登记稳定提示词合同和适用任务范围，无教材题目解析继续明确关闭教材依据段；注册表目前只负责身份与观察，不会改写现有提示词或消息顺序。

### 图片资产与上下文安全

- 主模型生图资产改为不可变内容寻址记录：图片和清单原子写入，读取及最终采用前重新核验 SHA256；只有实际进入最终答案或题目的图片才标记为已采用，诊断附件继续沿用既有容量与保留周期。
- Token 计量补充工具 schema、历史工具结果、结构开销和图片成本，并用供应商实际 usage 做本地校准。上下文压力下只裁剪较旧的失败工具结果正文，保留调用/结果身份、最近失败、成功结果、题干、答案约束和全部确认依据，不调用额外模型生成摘要。
- 图片工具参数错误、供应商失败、预算耗尽及重复调用提醒继续作为结构化结果返回同一主模型；无图任务不会因此增加图片调用或额外模型轮次。

### 更新可靠性

- Windows 或区域网络遇到连接重置、超时、限流和 GitHub 5xx 时，更新清单会有限重试；Raw GitHub 仍不可用时切换同仓库 GitHub Contents API，不使用第三方镜像。
- 源码 ZIP 下载支持断线续传；服务器忽略 Range 时安全从头重下。所有路径仍强制核对声明大小和 SHA256，重试耗尽或校验失败会删除未验证临时文件，不进入源码替换。

### 发布边界

- 锁定 Python 3.11 完整本地门禁通过 1899 项，分支覆盖率 70%；Python 编译、公式转换、Ruff、Mypy、许可证和项目完整性检查全部通过。
- 本版本不新增付费模型调用，不包含或覆盖 API Key、任务、教材、日志和历史输出；只读观察报告和诊断信息保存在用户数据目录，不进入源码包。

## [0.9.35] - 2026-08-29

### 题目解析与自主配图

- 辅助工具新增“题目解析”：复用真题解析的答案、审查、修复和 Word 交付核心，但不要求教材，不执行教材索引、检索或依据绑定；旧真题任务继续保持有教材模式。
- 真题解析、按题出题和知识点出题均可在“主模型自主生图”与“传统程序绘图”之间按任务切换。主模型根据完整题意自主决定是否生图，程序不再通过题型、关键词或第三方规则代替判断。
- 主模型可把任务内原图作为 `referenced_image_paths` 交给图片模型：无参考图时从零生成，有参考图或修正最近生成图时执行图片编辑，单次最多 5 张并保持原始像素；图片返回同一主模型检查并明确采用后才能进入结果。
- 已确认的配图意图不会再被来源图或旧绘图规则覆盖；默认教学图使用黑白、灰度和白底，只有题意明确要求颜色时才以颜色承载语义。

### 模型与图片服务

- 按 Codex 与 DeepSeek Harness 收紧原生工具闭环：按服务商、模型和实际协议登记能力，Gemini、Responses 与兼容 Chat 模型分别使用已验证路线；重试、修复或模型切换缺少等价能力时明确失败，不再静默变成纯文本或传统绘图。
- 灵算路线切换到当前可用入口并修复 Cloudflare 1010 误分类；OpenAI 图片 Key 独立使用 `gpt-image-2`。火山方舟新增图片专用入口，只展示两个 Seedream 5.0 模型，并关闭水印与组图输出。
- 已生成图片在答案修复、单题重生成和视觉修复中继续按同一事务回灌检查；超宽聚合反应式会按反应箭头安全换行，不再把原题图当作答案图。

### 发布边界

- 锁定 Python 3.11 完整本地门禁通过 1849 项，分支覆盖率 69%；Python 编译、公式转换、Ruff、Mypy、许可证和项目完整性检查全部通过。
- 本版本仍通过源码 ZIP 更新，不包含或覆盖 API Key、任务、教材、日志和历史输出；最终候选未重复发起付费图片编辑请求。

## [0.9.34] - 2026-08-29

### 生题修复检查点

- 生题的每次有效修复都会成为下一轮检查点，并以最新候选结果重新计算当前问题；不再回退到首版、重复报告已经修好的旧问题。
- 修复预算耗尽时，任务保留最后一份候选成果及对应诊断；已通过的同批题目不受影响，每题最多四次生成的成本上限不变。

### GLM 限流恢复

- BigModel/GLM 请求在网络边界共享并发上限；遇到可恢复的 429 时，按服务商 `Retry-After` 或带抖动的指数退避设置全局冷却，避免其他 GLM 线程继续冲击限流。
- GLM 429 仍记录为真实网络尝试，但不再误消耗题目内容生成次数；持续限流时也不再拆成逐题无效请求。
- 运行监控增加提供商并发上限、等待数、冷却时间和 429 计数，便于区分服务商限流与题目内容质量问题。

### 发布边界

- 本版本不改变纯文本主模型与识图阶段的交接范围，不改变题干配图的现有路由，也不新增付费模型调用。
- 本地 Python 3.11 完整门禁通过 1779 项，分支覆盖率 69%；更新不会包含或覆盖 API Key、任务、教材、日志和历史输出。

## [0.9.33] - 2026-08-28

### 产出结构观察与生题恢复

- 蓝图、生题规范化结果和绘图规格新增 Pydantic 影子观察：只在本机记录脱敏字段路径、错误类型、误报复核和耗时，不阻断任务、不修改结果，也不增加模型调用、Token、网络请求或重试。
- 环境页可查看影子样本和汇总报告；程序不会自动把影子规则升级为警告或阻断。未来即使经用户确认启用，也只允许确定性结构错误阻断，语义风险继续交给现有质量门。
- 生题模型阶段和完整结果增加后处理检查点；结果组装或历史保存异常后可复用已经完成的题目，避免重复生成。Word 公式预检和跨材料蓝图问题只定向修复受影响题目或蓝图项，不整体重写已通过内容。
- 生题失败诊断通过统一任务接口提供脱敏的阶段、检查点、失败类型和恢复能力，不公开请求载荷或完整结果检查点。

### 源码依赖与公式资源

- NumPy 改为直接依赖，增加 macOS/Windows 在 Python 3.9 和 3.11 下的精确源码运行约束；CI 会实际安装四种组合、执行 `pip check` 和关键导入冒烟。
- 增加第三方包用途与许可证清单；环境页非阻断提示实际依赖版本偏差，版本偏差本身不会让可运行环境启动失败。
- MathJax 源码资源从完整目录精简为实际使用的 TeX/MathML → CHTML 链路、扩展和官方字体，并修复异步启动与重复排版；Word 原生公式和网页公式能力保持不变。

### 发布边界

- 本版本仍是源码 ZIP：macOS 双击 `start_platform.command`，Windows 双击 `启动平台.bat` 后启动本地服务并打开网页；不要求桌面安装包。
- 本地完整门禁通过 1765 项，覆盖率 69%；未执行新的付费模型回放，不新增模型质量结论。更新不会包含或覆盖 API Key、任务、教材、日志和历史输出。

## [0.9.32] - 2026-08-28

### 任务页面与材料选择

- 真题材料页取消重复流程说明，把教材和真题整理为两个明确步骤；已有真题支持搜索、结果计数和分批展开，选择与删除使用独立操作，避免误删。
- 任务管理只保留一套状态筛选，并明确区分“新建任务”和“筛选任务”；任务卡默认展示一个主操作，其余操作收进“更多”，同时修复点击“更多”误打开任务详情的问题。
- 生题结果页默认收起原题诊断、训练蓝图和筛选条件，让第一道生成题更早出现；需要时仍可展开完整任务背景。

### 环境状态与一致性

- 环境页分别展示运行依赖、网络连通、API 与模型配置、当前模型实际调用状态，不再把“网络可达”误表达为“模型调用成功”；新增当前模型组合的直接连接测试入口。
- 统一任务页卡片、按钮、次要文字、禁用状态和键盘焦点样式，减少低效重复信息；桌面端 125% 与 150% 等效缩放下保持主要流程可用。
- 项目完整性审计改用稳定的功能标识检查结果下载和复核入口，不再因用户文案优化产生假失败。

### 发布边界

- 本版本仅调整网页端信息架构、交互和视觉一致性，不修改模型路由、提示词或上下文预算；未新增付费模型质量结论。
- 更新不会包含或覆盖 API Key、任务、教材、日志和历史输出。

## [0.9.31] - 2026-08-28

### 三条业务线的质量与交付

- 灵算 Google 新增 Gemini 3.7 Flash 低、中、高推理三个实际模型入口；保留既有默认模型和本地 Key，不会因升级强制改选。低推理档已完成真题解析、按题出题和知识点出题的固定小样本验证。
- 按题与知识点题目卷只展示明确标记为题干给定条件的结构化公式，不再把要求学生推导的关系式附在题目后方直接泄露答案；旧导出缓存同步失效。
- Word/PDF 一致性审计现在分别核对普通文字、公式、表格文字和图片，并明确报告 `compared` 或 `not_comparable`，不再把零文本锚点静默当作已完成比较。

### 失败恢复与用户体验

- 真题流程在教材检索失败后重试时，可安全复用与题面、教材集合、模型路由和政策完全一致的题面提取、题目理解、知识规划与图件规划，避免重复模型调用；任何输入或策略变化都会重新计算。
- 失败的真题任务显示真实停止阶段和未完成进度，不再以“失败 100%”误导用户；文件路径和日志事件默认收进技术详情，常见检索错误改为可理解的中文提示。
- 修复按题出题页面“保留并新建”“恢复上一份”按钮无反应的问题：空白自动草稿不再伪装成可恢复历史，操作失败会显示原因，任务详情也不会被后台任务列表刷新覆盖。

### 发布边界

- 本次仍为双击 BAT 启动本地网页的源码版本，不是桌面 App/EXE；不会包含或覆盖 API Key、任务、教材、日志、模型诊断和历史输出。
- 三线真实验证属于受控小样本。专项生题仍按现有产品契约只生成题目卷、不生成答案卷；默认语义审查的成本与答案闭环方案留待下一阶段决策。

## [0.9.30] - 2026-08-27

### Windows 启动修复

- 修复 Windows 转交到专用 Python 环境时，因 `%LOCALAPPDATA%\Answer Book Platform` 路径含空格而被拆成错误参数、双击 BAT 无法启动的问题。
- Windows 根目录新增显眼的 `启动平台.bat`，英文入口继续兼容；两个入口统一经过可记录失败原因的标准库引导器，不再依赖 PowerShell 或静默退出。
- 启动器未能显示时会弹出错误并给出 `launcher-bootstrap.log` 位置，便于直接定位用户机故障。

### 启动器识别与发布包

- 启动页、首次准备窗口、系统托盘和 Windows 桌面程序统一使用项目书本 Logo；Windows EXE 内嵌多尺寸图标。
- 正式源码附件改用白名单打包器生成，并在发布前反向检查启动文件、Logo、版本清单及敏感/运行数据排除规则，不再直接发布未筛选的 Git 归档。
- GitHub 发布页明确标出正确附件，防止普通用户误下页面底部自动生成的 `Source code (zip)`。
- 本次更新不包含或覆盖 API Key、任务、教材、日志和历史输出。

## [0.9.29] - 2026-08-27

### 模型路由与上下文质量

- 新增统一的模型能力登记表，按“服务商 + 模型 + 任务阶段”明确文本、图片、结构化输出、上下文和输出预算，不再依赖模型名称猜测能力。
- 真题解析、教材证据、答案生成、选择性复核、格式修复和生题流程按实际任务动态组装上下文，只输入当前阶段需要的材料，并为高风险或高质量阶段保留更充足的输入输出预算。
- 生题流程增加独立的上下文规划、批次预算和降载策略；材料过长时优先保留题面、考点、证据和必要图片，避免把冗余材料整包发送导致成本、耗时和失败率同时上升。

### 模型接入与默认组合

- 新增 Google AI Studio Gemini 与智谱 BigModel 官方接口配置，并统一模型错误分类和用户可执行的处理提示。
- 按题出题和知识点出题默认使用灵算 Gemini 3.6 Flash，作图兜底默认使用灵算 GPT-Image-2；两条业务线分别保存主模型、读图回退和生图模型，仍可手动调整。
- 真题解析保留“质量优先”和“性价比”两套方案；质量优先默认由 GPT-5.6 Terra 完成主要解析与读图，GPT-5.6 Sol 只复核高风险题，两套方案均使用 GPT-Image-2 生图。
- 用户界面不再展示火山方舟、阿里云百炼、商汤 SenseNova、OpenRouter、灵算 xAI 和灵算 Anthropic 的配置及模型入口；底层兼容和已有任务数据继续保留。

### 失败恢复与使用体验

- 生题批次允许保留已成功题目并对失败项给出明确原因和继续操作，不因单题异常丢弃整批有效产出。
- 优化模型连接、配置缺失和生成失败的提示层级，默认展示用户可理解的原因与建议，技术细节按需展开。
- 本次更新不包含或覆盖用户 API Key、任务、教材、日志、模型诊断和历史输出。

## [0.9.28] - 2026-08-27

### 发布门禁修正

- 明确公式边界收缩结果的可空类型处理，使锁定 Python 3.11 的 Mypy 门禁与实际非空分支保持一致；运行逻辑不变。
- 业务功能与前两个候选版本一致；0.9.26、0.9.27 失败标签继续保留，不移动、不复用。
- 发布手册补充逐层门禁经验：修复当前阻断项后，必须继续运行到完整工作流结束，不能以“上一层已通过”判断发布完成。

## [0.9.27] - 2026-08-27

### 发布门禁修正

- 修正环境探测模块的 import 区块格式，使锁定 Python 3.11 正式发布门禁能够继续执行。
- 业务功能与 0.9.26 候选版本一致；由于 0.9.26 标签的正式门禁未通过，本版本使用新的补丁号发布，未移动或复用失败标签。
- 补充发布手册：要求发布前实际执行 Ruff，并记录标签自动触发异常时的安全恢复方式及失败标签处理规则。

## [0.9.26] - 2026-08-27

### 产出质量与教材证据

- 修复英文说明句被整句误识别为 Word 公式的问题；普通文字继续保持可读文本，只有明确的局部公式进入原生公式对象。
- 选中部分题目导出时重新执行整套重复度门禁，避免从合格题集里挑出高度重复子集后仍被当作正式结果交付。
- 教材证据确认未调用或异常时，只保留有序候选供复核，不再把检索第一项伪装为“已确认/直接支持”的正式教材依据。
- 修复 DOCX 教材被当作二进制文本索引、产生大量空块却显示成功的问题；现在可确定性提取 Word 段落和表格，同时继续明确标记页码未经验证。

### 流程效率与失败恢复

- 按题出题和知识点出题在提交前检查模型与 API Key；缺少配置时不再创建注定失败的任务，当前材料和设置保持不丢失。
- 缓存 Word 与绘图运行环境的短期静态检查，连续打开环境页时避免重复执行数秒级探测；供应商、Key 与网络状态仍实时检查。
- 当全部答案都已明确不可交付时，保留答案片段、质量审计、失败诊断和重跑能力，但跳过无意义的隐藏 Word、PDF 与 PNG 构建；需要文档取证时可显式保留原流程。
- 命令行任务会持久化本次无模型、渲染和检查点参数，服务恢复时不再误用旧默认值产生额外模型调用或文档工作。
- 模型被配置为仅使用 Responses 协议时不再偷偷切换到 Chat Completions；供应商并发默认值本次未调整。

### 使用体验与状态表达

- “按题生题”统一为“按题出题”，优化移动端导航、失败/取消任务卡片和未发起模型调用时的进度表达。
- 草稿恢复改为冻结进入页面前发现的候选；新输入的自动保存不会覆盖旧草稿，恢复可能替换当前输入时会先明确确认。
- API Key 保存后，供应商卡片立即显示“已配置”，不再同时出现“已保存”和“等待测试”的矛盾状态。
- 修正真题材料页指向不存在入口的说明；知识点名称、正文和文件均为空时禁用提交，错误焦点不再跳到隐藏的按题输入框。

### 验证、升级与已知限制

- 已完成相关跨线回归、真实浏览器检查及受控模型连接/知识规划验证；发布标签仍必须通过锁定 Python 3.11 的完整质量门禁和源码 ZIP 独立启动检查。
- 已确认但留待下一阶段处理：Windows 超长教材路径、无印刷页码文本材料的候选证据策略，以及失败任务中阶段、题目计数和总进度的展示一致性。
- 新增正式发布操作手册，记录环境选择、跨平台路径、版本同步、打包反向验证、标签触发和发布后核验经验。
- 本次更新不包含或覆盖 API Key、任务、教材、日志、模型诊断和历史输出。

## [0.9.25] - 2026-08-26

### 出题流程与页面结构

- 将按题出题和知识点出题统一为清晰的 5 步流程，让用户从提交材料、解析考点、确认范围、审查蓝图到生成练习始终知道当前位置和下一步操作。
- 精简首次提交页面，只保留材料、模型和主要操作；材料说明、高级生成设置及低频识别修正默认折叠，减少无关信息干扰。
- 优化范围确认和蓝图审查页面的信息层级，只默认展示当前决策需要的内容；分析依据、覆盖明细和批量生成参数可按需展开。

### 任务恢复与异常反馈

- 从任务管理恢复按题出题时自动定位到正确步骤，避免恢复后停留在错误位置或需要重新寻找操作区域。
- 任务卡片优先展示业务状态、当前阶段、进度和下一步；流程 ID、阶段 ID 与请求次数统一收纳到“运行详情”。
- 生成失败时先给出用户可理解的原因和处理方法，模型协议、返回序号等内部诊断改为按需查看的技术详情。

### 桌面网页适配与验证

- 改进 1024px、1280px 和 1440px 等桌面窗口宽度下的导航、表单、底部操作栏与任务卡片布局，避免横向溢出和按钮拥挤。
- 全量回归验证通过 1614 项，另对最终信息层级改动完成 153 项针对性回归；本次更新不包含或覆盖用户 API Key、任务、教材、日志和历史输出。

## [0.9.24] - 2026-08-26

### 模型接入与兼容性

- 按“服务商 + 具体模型”分别选择接口、思考强度、输出长度和支持参数，不再对同一服务商的所有模型套用相同协议。
- Gemini 改用 Chat Completions，并严格区分思考过程与最终答案；Claude 优先使用官方 Messages 接口和结构化 JSON 输出。
- 修复百炼视觉模型、OpenRouter 的 GLM/MiniMax 以及火山方舟部分 GLM/Kimi 模型的接口兼容问题；MiniMax 自动剥离思考标签。
- 修正 DeepSeek Responses 地址及不支持的 `store` 参数；`qwen-vl-ocr` 最大输出单独限制为 4096 tokens。
- 灵算接入模型统一恢复官方默认思考强度；重试降级时 Gemini 最低使用 `minimal`，Grok 最低使用 `low`，不再发送各模型不支持的采样参数。

### 生题稳定性与重试

- 延长首个响应等待和单次生成时限，允许复杂题目在约 5～8 分钟内完成，减少慢响应误判。
- 连续失败后暂停 20 秒再进行单次恢复探测；服务恢复即继续生题，熔断期间不再对所有剩余题目重复发起无效请求。
- 每道题独立享有最多 4 次请求机会，不再与其他题目共享重试额度；JSON 格式错误也会按正常生成、降低思考、强化结构约束和兼容接口等策略重新尝试。
- 增强异常响应、控制字符、思考片段和公式包装的识别修复，避免模型已经返回有效内容却被错误判定为整题失败。

### Word 下载与公式修复

- 修复专项练习下载重复触发、下载到空 Word 文件，以及历史成功任务无法正常导出的问题；浏览器只会下载一次经过服务端校验的有效文件。
- 修复填空题公式后的横线、网关返回异常 Unicode 控制字符以及历史题目公式包装问题，确保题目正文、答案和原生 Word 公式完整可见。
- 导出前增加文件内容与公式可渲染性检查；无内容或无效文档会被明确拒绝，不再交付空白文件。

### 验证与升级说明

- 使用用户原始截图、原始任务配置分别复测 Luna 与 Gemini，两项任务均完整生成 15/15 道题；历史任务及新任务的 Word 下载均已验证有正文和原生公式。
- 本地回归验证通过 1588 项；本次更新不包含用户 API Key、运行中任务、教材、日志或历史输出，升级继续保留现有用户数据。

## [0.9.23] - 2026-08-25

### 新增

- 新增“元衡 API”服务商，使用流式 Responses API，可在 API 配置页独立测试和保存 `YUANHENG_API_KEY`。
- 仅接入已完成真实请求验证的 `gpt-5.6-luna` 和 `gpt-5.6-terra`，两者均支持文本、严格 JSON Schema、图片理解和 SSE 流式输出。

### 修复

- 修复 B.AI 开启 Cloudflare 客户端特征检查后，软件正常模型请求被误拦截为 `403 / error 1010` 的问题。
- 浏览器兼容 `User-Agent` 仅对 B.AI 启用，不改变其他服务商的默认请求头。

### 验证与安全

- 修复后已用软件自身客户端真实调用 B.AI `deepseek-v4-flash`，成功返回预期内容。
- 新增服务商的 Key 仍只保存在系统用户数据目录，程序更新不会覆盖，测试 Key 不会写入仓库或发布包。

## [0.9.22] - 2026-08-25

### 新增

- 新增 OpenRouter 服务商，可在“API 配置”页面独立测试和保存 `OPENROUTER_API_KEY`。
- 首批接入 `stealth/ox-alpha`，支持文本与图片理解，并使用已验证的流式 Responses API。
- 新增 `z-ai/glm-5.2:free` 文本模型和 `minimax/minimax-m3:free` 多模态模型。
- 用户可见任务名称会将三个模型分别简写为“Ox Alpha”、“GLM”和“MiniMax”。

### 验证与注意事项

- Ox Alpha 的普通文本、JSON 结构化输出和程序实际流式 Responses 调用均已通过真实请求验证。
- OpenRouter 官方模型信息声明 Ox Alpha 支持图片输入；测试期间 Stealth 上游共享池曾对图片请求返回临时 429，遇到时应稍后重试。
- MiniMax M3 Free 的文本、JSON 结构化输出和图片理解均已通过真实请求验证；GLM 5.2 Free 模型可见，但测试时上游免费共享池持续限流，暂未完成生成成功验证。
- API Key 仍只保存在系统用户数据目录，程序更新不会覆盖，也不会写入任务产物或发布包。

## [0.9.20] - 2026-08-25

### 新增

- 新增商汤日日新服务商，接入 `sensenova-6.8-flash-lite`、`deepseek-v4-flash`、`glm-5.2` 和图片模型 `sensenova-u1.5-lite`；按要求不提供 `sensenova-u1-fast`。
- 新增 B.AI 服务商，仅接入真实请求已通过的 `deepseek-v4-flash`、`deepseek-v4-flash-vision-exp`、`hy3` 和 `mimo-v2.5`。
- 商汤 6.8 Flash Lite 与 B.AI Vision Exp 可作为图片理解模型；商汤 U1.5 Lite 可生成并立即保存 2K PNG 题图。

### 更新体验

- 更新前拦截运行中与排队任务，避免服务重启中断生题。
- 服务退出后由独立安全更新窗口展示解压、备份、覆盖、依赖检查、启动和失败回滚状态。
- 修复更新失败后反复重试、网页重复打开以及原程序目录可能短暂为空的问题。
- 保持 API Key、教材、任务、日志和输出位于独立用户数据目录，升级与失败回滚均不覆盖用户数据。

## [0.9.19] - 2026-08-24

### 重要升级说明

- 启用 `update-stable-v2.json` 安全更新源，源码替换前先在安装目录旁准备完整新版本并备份旧源码。
- 0.9.18 及更早的源码 ZIP 仍使用旧替换器，需要手动下载安装 0.9.19 或更高版本一次；旧更新源保持停用。
- 新启动器在首次运行时自动创建用户级 Python 环境并安装依赖，缺少 Python 时仅提示用户安装。
