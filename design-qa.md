## 2026-09-16 本次调整（覆盖下方旧设计结论）

教材引用与真题解析同级，另列必选生图模型；桌面三列，中等宽度两列，小屏单列。独立读图/复核配置隐藏，环境自检不再在本页重复。已完成自动回归、服务/API 检查及内置浏览器约 835×720 视口截图检查：教材引用与真题解析并列，生图换至下一行，独立读图/复核配置不显示。未完成手机窄屏交互验收，下方历史截图不能作为本次验收依据。

# 模型配置页视觉验收

- source visuals:
  - `/Users/ljj/.codex/generated_images/01a0898f-2c5f-7cf0-a38e-4bfc75e40750/exec-5430ea4e-6d37-4d3f-aa30-071d4befeeb4.png`
  - `/Users/ljj/.codex/generated_images/01a0898f-2c5f-7cf0-a38e-4bfc75e40750/exec-386b4dce-fbf7-4b24-8fcb-87183cfa2178.png`
- implementation screenshot: `/tmp/answer-book-model-config-implementation.png`
- viewport: 1470 × 742，桌面 Chrome，本地服务 `http://127.0.0.1:8766/`
- state: 真题解析 / 环境配置；环境自检完成；主要下拉关闭。另单独打开“思考强度”验证选中标记。

## 对照结论

- 页面延续参考图的左右工作区：左侧为环境自检，右侧为模型方案。
- 主要模型采用宽横向任务行；任务说明、供应商、模型、请求方式、思考强度与状态形成稳定阅读顺序。
- 供应商与模型共享主要选择区域，请求方式与思考强度降为右侧次级参数，状态移至第二行，避免五列压缩造成截断。
- 视觉输入、生图与高风险分工保留渐进展示，不与两个常用主模型争夺首屏。
- 桌面宽度无横向溢出；1470 px 视口下 `scrollWidth` 为 1463 px。

## 交互与状态验收

- 环境检查完成后不保留可见旋转加载图标，成功态使用静态完成标记。
- 两个主要模型的默认思考强度均为 `medium`。
- 当前 GPT/兼容路线按能力记录使用 Responses；协议控件只有一种可用方式时保留可见但禁用，用户仍能理解实际调用方式。
- 思考强度菜单只在当前选中的 `中（medium）` 项显示一个对勾，其余选项不显示对勾。
- 生图供应商列表仅显示一个“火山方舟”，并保留阿里云百炼及 WawAPI 图片路线。
- 浏览器控制台无 error 或 warning。

## 迭代记录

1. 初版仅将原卡片压成五列表格，与参考图的宽主选择区差异明显，且状态列和模型名容易截断。
2. 二次调整取消五列表头，改为参考图 1 的宽任务卡结构，并吸收参考图 3 的横向参数组织；恢复字段标签、将状态放到第二行、扩大供应商/模型主区域。
3. 检查下拉选中态、自检完成态、无横向溢出和控制台日志后通过。

final result: passed

## 生图行精简复核（2026-09-12）

- source visual: `/var/folders/4h/hb2llyk50qv2zftj7m5pd1x80000gn/T/codex-clipboard-e8b18908-f95a-4c14-91c0-024d6fc7530e.png`
- implementation screenshot: `/tmp/answer-book-image-row-implementation.png`
- 修正: 删除无操作意义的“按需调用”和“—”占位块，生图行仅保留供应商、模型与路线状态；选择区域使用完整可用宽度。
- DOM 验收: 生图行 `.model-role-static` 数量为 0，控制区为单列 679 px，页面无横向溢出。
- 浏览器控制台: 无 error 或 warning。

final result: passed

## 任务模型选择页密度复核（2026-09-16）

- 用户反馈图：`.artifacts/design-density/01-before-request-controls.png`
- 验收范围：真题解析、按题出题、知识点出题的任务开始前模型选择，以及共享标题、卡片、表单控件和操作区。
- 按题出题实测视口：842 × 726；页面 `scrollWidth=835`，无横向溢出。
- 调整前：页面总高度约 1826px；模型卡 715px；模型路线 62px/行；请求方式和思考深度下拉 54px。
- 调整后：页面总高度约 1615px；模型卡 617px；模型路线 50px/行；请求方式和思考深度下拉 44px。
- 结果：总滚动距离减少约 12%，主模型与生图模型仍为同级必选；家族筛选、型号＋服务商路线、请求方式、思考深度、状态提示和自动保存说明均保留。
- 交互复核：按题与知识点页面均正常加载已配置路线；新建真题任务保留用户现有模型选择，不再自动覆盖为固定组合。
- 可访问性边界：桌面控件保持 44px 关键点击高度；窄屏家族标签提升为 40px，模型路线保持 50px；键盘焦点样式继续保留。截图不能单独证明完整无障碍合规。
- 验证：定向回归 233 passed；Node 语法和 diff 格式检查通过。

final result: passed

## 任务模型选择页去重复检查（2026-09-16）

- source visual: 用户提供的 Chrome 页面截图（左侧环境自检占据模型选择空间）。
- implementation: 本地 Chromium `http://127.0.0.1:8877/?page=env`。
- viewport: 当前 Codex in-app browser 桌面视口；DOM 复核确认页面无环境检查栏。
- state: 真题解析 / 选择模型；主模型与生图模型均显示为必选，协议和思考强度可见，高级模型分工默认收起。
- findings: 原页面内的启动环境、依赖、网络与模型调用复查已移除；模型选择主体现在使用完整宽度，底部提示改为“本次任务模型已选择；开始任务时会验证实际连接”。
- responsive: 保留现有模型卡窄屏堆叠规则；环境检查不再产生额外滚动区域。
- limitations: 当前本地预览使用隔离数据目录，未执行真实模型调用；模型连接验证仍在任务开始时执行。

final result: passed
