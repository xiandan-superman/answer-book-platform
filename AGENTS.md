# 项目协作要点

## 平台背景与研发目的

- 这是给个人/小团队使用的本地 AI 教学平台，三条核心业务线是：真题解析、按题出题、知识点出题。平台的目标不是展示模型或堆叠功能，而是把用户材料稳定转化为内容正确、结构完整、可继续编辑且可核验的 Word/PDF，并保留足以解释结果和定位失败的过程证据。
- 程序负责流程、状态、预算、确定性校验、恢复和交付，模型只负责适合由模型完成的理解、判断与生成。外部模型存在能力差异、输出波动、限流和费用，研发时必须把这些不确定性纳入流程设计，不能把一次调用成功当成任务成功。
- 优化优先级依次是最终产出质量、任务完成率与用户可用性，其次才是流程效率和视觉体验；代码整洁与架构调整只在能降低缺陷、风险或维护成本时实施，避免高风险低收益改动。

## 分析、调整与修理规则

- 动手前先确认最新工作树、实际业务流程和现有证据，不凭旧目录、旧压缩包、单个页面或单个报错猜测。先用用户结果描述问题，再明确触发场景、根因、受影响范围、必须保持的质量/失败率/费用/数据边界以及验证方法。
- 修复方案确定后必须统筹检查真题解析、按题出题、知识点出题及共享后处理中的同类环节，先判断是共享根因还是单一入口问题，再确定最小但完整的修改范围和回归集；禁止只为当前样本、当前测试或当前业务入口打补丁。
- 每次调整或优化必须形成可核验的跨场景影响矩阵：逐项检查真题解析、按题出题、知识点出题、历史任务恢复、最终 Word/PDF 交付及相关共享基础设施。对同一根因适用的入口必须统一修复并分别提供回归证据；判定不适用的必须记录代码或合同证据。只验证一个任务类型、一个页面或一个用户样本时，不得宣称平台级问题已完成。
- 修改共享合同、阶段产物或流程状态时，必须检查生产者、消费者、持久化、任务恢复、前端展示和最终交付是否仍然一致。不得为了让测试通过而降低业务门禁、隐藏错误、静默丢失内容或改变用户已经确认的结果。
- 多轮生成、审查和修复流程必须以最新有效候选为下一轮输入，并按该候选重新计算当前问题；已解决的旧问题不得因回退到初始版本而被重复报告。如为保护正式交付而不能直接接受未通过候选，也必须保留其作为后续修复检查点，最终诊断必须对应最后检查的版本。

## 模型与多模态修改规则

- 模型与多模态链路的设计、修复和降级必须以本项目的用户输入、持久化产物、工具结果、质量门和交付合同为直接依据。能力门只决定能否安全暴露工具，负责当前内容的主模型决定是否调用，真实工具结果必须回到当前任务上下文，只有已校验并由主模型明确采用的资产才能交付。
- 模型能力以机器可读登记为准，并按服务商通道、模型和协议判断；不得根据名称、服务商级开关或连接成功猜测视觉、结构化输出、工具调用和任务适配能力。详细合同以 `docs/MODEL_PROVIDER_INTEGRATION_STANDARD_DRAFT.md` 为准。
- 任务必需的文字、图片及来源映射必须贯穿解析、生成、修复、恢复和诊断；不得静默丢弃必要模态、伪造证据或绕过门禁。模型切换、输入转换和降级必须保持当前阶段所需能力等价，网络成功后仍须验证结构、业务质量及适用时的图文一致性。
- 模型调用、重试、工具循环和多轮回灌必须有明确预算。生成图片只有在真实资产已校验、回传并由负责当前内容的主模型明确采用后才能进入结果，不能用提示词、占位符、路径或未经检查的资产 ID 代替真实图片。

### 模型故障的分层归因流程

- 排查模型任务时必须沿“用户原始输入 → 实际模型请求 → 原始模型响应 → 工具结果回灌 → 本地确定性变换 → 质量门 → Word/PDF 交付”逐层保留证据并做最小复现。异常栈只能定位失败位置，不能单独作为根因结论。
- 每个问题必须明确归入一个主责任层，并列出可能的次要诱因：`model_input`（缺失、截断、模态或来源映射错误）、`model_output`（结构或语义错误）、`provider_transport`（超时、限流、网关或连接错误）、`model_orchestration`（工具暴露、结果回灌、重试、状态或恢复闭环错误）、`deterministic_postprocess`（程序在模型返回后改坏正确内容）、`delivery_environment`（Word/PDF 转换器、字体、依赖或系统能力）。不得把“模型参与过”直接等同于模型错误，也不得把供应商波动当成所有后续失败的解释。
- 必须分别验证“原始模型输出是否已满足合同”和“经过每一层本地变换后是否仍满足合同”。如果原始输出可被当前权威转换器或校验器接受，而后处理产物失败，主因属于程序设计；禁止通过重新提示模型、降低质量门或增加重试掩盖确定性代码缺陷。
- 修复顺序固定为：先修不应由模型承担的确定性程序错误；再补工具错误的结构化、模型可见回灌和有界重试；最后才调整模型输入合同或提示。程序能够无损识别并修复的问题不得额外消耗模型调用，模型语义问题不得用本地猜测静默改写。
- 排查结论至少包含：直接失败原因、触发条件、主责任层、原始输入/输出是否正确、受影响场景、实际需要修复的项目和验证矩阵。调用次数、失败率和最终内容问题要分别统计，避免把相关性写成因果关系。
- “排查”不自动授权“修复、重跑、部署或发布”。用户要求先记录或等待时，只能保存脱敏问题记录和方案边界；取得明确开始指令后才能修改业务实现。当前待处理问题及实施边界记录在 `docs/operations/KNOWN_ISSUES.md`。

## 修改完整性与权威入口

- 服务商、模型、协议或能力变化必须同步公开配置、`config/model_capabilities.json` 和由其生成的能力文档；依赖变化必须同步受影响的 requirements、跨平台 constraints、第三方许可证记录和安装验证，禁止只改链路中的一个文件。
- `app/`、`web/`、`config/`、`scripts/`、`tests/` 和 `docs/operations/` 分别承载业务实现、界面契约、能力/配置、工程工具、回归证据和操作规范；修改时按责任定位影响面，但不得用目录边界代替端到端业务判断。
- 开发启动、质量门禁、数据保留、源码分发和发布的现行命令与矩阵分别以 `README.md`、`docs/operations/QUALITY.md`、`docs/operations/DATA_RETENTION.md`、`docs/SOURCE_DISTRIBUTION_AND_UPDATE.md` 和 `docs/operations/RELEASE.md` 为准；AGENTS 只保留长期判断和修改原则，不复制容易过时的操作细节。

## 不可破坏的边界

- 任何代码、依赖、配置或流程改动开始前，必须完整阅读 `docs/operations/OPTIMIZATION_LOG.md`；完成后必须在该文档顶部追加一条记录，简明写清“改了什么、为什么改（错误场景或考虑角度）、质量/失败率底线、验证结果”。没有补充记录的改动视为未完成；纯调查且未改文件时无需记录。
- API Key、任务、教材、日志和输出保存在系统用户数据目录；源码目录可整体替换。迁移只能补缺，更新失败必须保留备份并回滚，绝不覆盖已有用户数据。
- 更新清单和正式附件遇到连接重置、超时、限流或服务端瞬时故障时必须有限重试；清单备用入口只能读取同一受信 GitHub 仓库，附件续传或重新下载后仍须完整通过声明大小和 SHA256，重试耗尽必须删除未验证临时文件，不得进入源码替换。
- 修改后按影响范围选择验证层级：纯文案或局部无逻辑改动至少检查格式与引用；单模块逻辑改动运行定向回归；共享生成、解析、存储或后处理改动覆盖三条业务线中的相关入口；启动、更新、配置或发布改动必须做真实启动/制品验证；正式发布候选执行完整门禁。最终汇报必须列出实际命令、结果、未覆盖项及原因，不得用 `py_compile` 或单个样本代替所需层级。
- 任务完成状态与发布状态分开报告，至少区分“源码已改、本地已验证、Git 已提交并推送、正式源码版已公开、桌面包是否另行发布”；当前任务未要求或未完成的状态必须如实写明，不得把本地修改或 Git push 简称为“已发布”。
- 当前普通用户交付渠道、支持平台和入口名称以 `docs/SOURCE_DISTRIBUTION_AND_UPDATE.md` 为准，不在本文件复制容易过时的包装细节。所有正式 Windows 源码制品必须提供可发现的启动入口，在普通用户权限及包含空格/中文字符的路径中可启动；失败必须写日志并显示可理解错误，不能静默退出。
- 修改启动或更新机制前完整阅读 `docs/SOURCE_DISTRIBUTION_AND_UPDATE.md`；发布前完整阅读 `docs/operations/RELEASE.md`，细节以这两份文档为准。

## 正式发布边界

- 正式版本必须由受保护的自动发布工作流产生，且标签只能指向已经通过当前完整门禁、制品反向验证和源码包启动冒烟的确定提交；测试矩阵、版本元数据、清单、附件校验和稳定更新源的现行细节以 `docs/operations/RELEASE.md` 及工作流配置为准，避免在本文件写死易变实现。
- 正常流程禁止人工创建或推送正式版本标签，也不得删除、移动、强推或复用历史标签。若托管平台或自动发布基础设施发生持续故障，任何人工应急发布都必须先取得用户明确授权，形成单独的可回滚方案和审计记录，并完成与正常流程等价的质量、制品与公开附件验证；没有这些条件时不得以“紧急”为由绕过门禁。
- 版本号未增加的普通 `main` push 只做质量检查，不重复发布。版本元数据的一致性必须由自动检查保证；公开发布完成只能以公开附件、校验值和稳定更新源均核验成功为准。

## 已踩过且不能回退的坑

- 定向测试、`py_compile` 或普通门禁不能代替发布手册规定的完整 Ruff/Mypy/pytest 门禁；Windows 路径比较要统一为 POSIX 形式。
- 正式 ZIP 只能由 Git 索引白名单打包并反向验证，禁止直接发布未筛选的 `git archive`；`.artifacts/`、运行数据和本机配置不得进入附件。
- Windows 启动链路不得使用已知会破坏路径参数或隐藏失败的进程交接方式；必须从正式入口、真实含空格/中文字符路径验证到健康检查与持续运行。当前已知实现禁区和回归样本以源码分发文档为准。

## 专项约定

- 收到 `AB-...` 反馈编号时，先运行 `python3 scripts/inspect_support_report.py <编号>` 并读取返回的诊断文件；网页摘要不能代替任务输入、模型上下文、阶段产物和日志证据。
- 用户可见任务名使用“任务类型 · 主模型简称 · 可区分内容”，不显示内部 ID、路径或技术文件名。
- Word 原生公式内所有可见字符统一斜体；DOCX 审计不得恢复化学式、单位或 `\mathrm` 的正体。
