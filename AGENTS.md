# 项目协作规则

## 本地审查与版本发布

- 程序修改完成后，先从当前源码启动一个独立的本地网页预览端口，供用户直接审查；不要为了查看小改动反复制作安装包。
- 本地预览必须使用独立端口和独立数据目录，不得影响用户当前安装版、已有任务、API Key 或正式数据。
- 用户确认修改符合预期后，再提交并推送 Git。
- 普通小改动只进入 Git，继续累积，不立即发布用户更新。
- 用户主分发方式是“源码启动版”，不是桌面安装包。普通用户从 GitHub 下载源码 ZIP，双击 macOS/Windows 启动文件；首次启动由 `scripts/source_launcher.py` 创建用户级虚拟环境并安装依赖，然后启动本地服务并打开浏览器。
- 正式更新只能由与 `APP_VERSION` 完全匹配的版本标签触发。禁止把每次 `main` push 直接推送给普通用户。
- 版本标签由 `.github/workflows/source-release.yml` 生成带固定顶层目录的源码 ZIP、SHA256 更新清单并发布到公开更新仓库。源码 ZIP 用户由启动监督器安全替换代码；Git clone 用户只允许 clean worktree、同分支、fast-forward 更新。
- 源码更新前后，API Key、任务、教材、日志和输出必须保存在系统用户数据目录，禁止重新放回可替换的仓库目录。更新失败必须保留备份并回滚。
- 依赖检查必须静默进行；首次启动自动安装。已运行用户若新版本改变依赖清单，网页确认更新时必须明确提示，重启后才安装。缺少 Python 时只提示并引导用户安装，不自动下载 Python 运行时。
- macOS/Windows 桌面安装包是可选兼容渠道，只能手动触发 `.github/workflows/desktop-release.yml`，不得作为普通源码用户更新的前置条件。
- 汇报时必须明确区分：源码已修改、本地预览已更新、Git 已推送、源码更新版已发布、桌面安装包已发布；不得把其中一个状态说成另一个状态。

默认流程：`修改程序 → 独立本地端口预览 → 用户确认 → Git 提交并推送 → 累积到版本 → 更新 APP_VERSION/VERSION/清单 → 创建版本标签 → GitHub Actions 发布源码更新 → 用户网页确认更新 → 启动器替换源码、检查依赖并重启`。

实现或修改启动更新机制前，必须完整阅读 `docs/SOURCE_DISTRIBUTION_AND_UPDATE.md`，并同步更新该文档和 README 中的用户流程。

## 用户反馈排查

- 用户给出 `AB-...` 反馈编号并要求排查时，不要先读取或操作浏览器中的问题反馈页面。
- 先在项目根目录运行 `python3 scripts/inspect_support_report.py <AB编号>`，再读取命令返回的 `diagnostic_files`。
- 必须结合任务输入、相关答案或中间结果、模型上下文、任务生命周期和错误日志判断根因；不得仅根据列表摘要猜测。
- 问题反馈页面只用于浏览、标记处理和复制排查指令，不是诊断数据源。
