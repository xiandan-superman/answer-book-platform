# 项目协作要点

## 项目定位与判断原则

- 这是给个人/小团队使用的本地 AI 教学平台，三条核心业务线是：真题解析、按题出题、知识点出题；正式结果以可核验的 Word/PDF 和过程证据交付。
- 优化优先级是产出质量与可用性，其次是流程效率和视觉体验；代码整洁与架构调整只在能降低缺陷、风险或维护成本时实施，避免高风险低收益改动。
- 动手前先确认最新工作树和现状，不凭旧目录、旧压缩包或单个页面猜测。涉及模型质量时使用有代表性的小样本真实测试；付费、外部发布或可能改变用户数据的动作按任务授权执行。
- 修复方案确定后必须统筹检查真题解析、按题出题、知识点出题及共享后处理中的同类环节，先判断是共享根因还是单一入口问题，再确定修改范围和回归集；禁止只为当前样本或当前业务入口打补丁。
- 多轮生成、审查和修复流程必须以最新有效候选为下一轮输入，并按该候选重新计算当前问题；已解决的旧问题不得因回退到初始版本而被重复报告。如为保护正式交付而不能直接接受未通过候选，也必须保留其作为后续修复检查点，最终诊断必须对应最后检查的版本。

## 不可破坏的边界

- 任何代码、依赖、配置或流程改动开始前，必须完整阅读 `docs/operations/OPTIMIZATION_LOG.md`；完成后必须在该文档顶部追加一条记录，简明写清“改了什么、为什么改（错误场景或考虑角度）、质量/失败率底线、验证结果”。没有补充记录的改动视为未完成；纯调查且未改文件时无需记录。
- API Key、任务、教材、日志和输出保存在系统用户数据目录；源码目录可整体替换。迁移只能补缺，更新失败必须保留备份并回滚，绝不覆盖已有用户数据。
- 修改后按风险运行定向回归、整库门禁或真实启动测试。必须明确区分“源码已改、本地已验证、Git 已推送、正式源码版已发布、桌面包已发布”。
- 用户主渠道是 GitHub 源码 ZIP，不是桌面安装包。Windows 必须保留显眼的 `启动平台.bat` 和英文入口；启动失败要写日志并显示可理解错误，不能静默退出。
- 修改启动或更新机制前完整阅读 `docs/SOURCE_DISTRIBUTION_AND_UPDATE.md`；发布前完整阅读 `docs/operations/RELEASE.md`，细节以这两份文档为准。

## 自动发布硬规则

- 禁止人工创建或推送正式版本标签。发布者只更新版本元数据和变更记录并推送 `main`；Python 3.9、3.11 和 Chromium 门禁全部成功后，`.github/workflows/source-release.yml` 才能自动创建标签、打包和发布。
- `APP_VERSION`、`VERSION`、`RELEASE_MANIFEST.json` 与 `CHANGELOG.md` 条目必须一致。版本号未增加时，普通 `main` push 只做 CI，不重复发布。
- 标签必须晚于打包反向验证和源码 ZIP 启动冒烟；上传使用有限重试和幂等恢复，结束时重新下载公开附件，核对 SHA256、大小及 `update-stable-v2.json`。不得删除、移动或复用历史标签。

## 已踩过且不能回退的坑

- 定向测试、`py_compile` 或普通门禁不能代替锁定 Python 3.11 的完整 Ruff/Mypy/pytest 门禁；Windows 路径比较要统一为 POSIX 形式。
- 正式 ZIP 只能由 Git 索引白名单打包并反向验证，禁止直接发布未筛选的 `git archive`；`.artifacts/`、运行数据和本机配置不得进入附件。
- Windows 默认运行目录含空格，禁止用 `os.execv` 交接图形运行时；必须从真实 BAT、真实含空格路径验证到健康检查。
- 0.9.18 及更早 ZIP 版需先手动安装 0.9.19；旧 `update-stable.json` 保持暂停，0.9.19+ 只使用 `update-stable-v2.json`。

## 专项约定

- 收到 `AB-...` 反馈编号时，先运行 `python3 scripts/inspect_support_report.py <编号>` 并读取返回的诊断文件；网页摘要不能代替任务输入、模型上下文、阶段产物和日志证据。
- 用户可见任务名使用“任务类型 · 主模型简称 · 可区分内容”，不显示内部 ID、路径或技术文件名。
- Word 原生公式内所有可见字符统一斜体；DOCX 审计不得恢复化学式、单位或 `\mathrm` 的正体。
