{
  "package_name": "answer_book_platform_v1",
  "product_name": "真题解析与生题平台",
  "version": "0.9.35",
  "version_source": "APP_VERSION",
  "source_revision": "release-v0.9.35",
  "build_kind": "stable-source-release",
  "created_at": "2026-08-29T00:00:00+08:00",
  "validation": {
    "pytest": "the locked Python 3.11 full local quality gate passed with 1849 tests and 16 configured deselections",
    "platform_quality_gate": "local compilation, full pytest and 69 percent branch coverage, version consistency, formula conversion, third-party notices, project completeness, Ruff and Mypy passed; locked Python 3.9/3.11, Chromium and macOS/Windows source-lock gates must still pass on main before the release tag is created",
    "version_consistency": "passed",
    "saved_real_task_replay": "the saved illustrated-question candidate and model-route diagnostics that triggered these fixes were inspected; deterministic replays confirmed the corrected intent and asset gates without repeating paid image-edit requests",
    "cross_subject_regressions": "true-exam, question-only analysis, per-question generation and knowledge-generation contracts passed in the full local gate; main-model image-tool generation, editing, inspection, retry and legacy-route isolation are covered by deterministic regressions",
    "paid_model_regression": "earlier scoped provider probes in this development cycle verified registered native tool calls and the generated-image inspection loop; no paid image-edit request was run for the final 0.9.35 release candidate",
    "cost_model_routing": "the main model alone decides whether to call the image tool; text-only tasks retain zero image calls, image edits do not silently fall back to new generation, and existing request budgets remain enforced",
    "local_credentials_included": false,
    "runtime_user_data_included": false
  }
}
